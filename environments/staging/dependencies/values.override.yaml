##################################################################################
# Environment-specific Helm overrides managed by yarn environment:init
# Unmanaged keys are preserved when this file is updated.
##################################################################################
hostname: farajaland-staging.opencrvs.org

monitoring:
  enabled: false

elasticsearch:
  host_data_path: /data/staging/elasticsearch

minio:
  host_data_path: /data/staging/minio

mongodb:
  host_data_path: /data/staging/mongodb

postgres:
  host_data_path: /data/staging/postgres

influxdb:
  host_data_path: /data/staging/influxdb